# Server
PORT=8080

# Auth (Better Auth)
# BASE_URL must match the public URL clients use to reach the server.
# On Render, set this to your service's URL (e.g. https://your-app.onrender.com).
CORS_ORIGIN=http://localhost:3000
BASE_URL=http://localhost:8080
ALLOW_REGISTRATION=false  # Set to true temporarily to create accounts

# Admin account (for scripts/create-admin.ts)
ADMIN_EMAIL=your-email@example.com
ADMIN_PASSWORD=your-secure-password
ADMIN_NAME=Your Name

# Email – Mailtrap Production API
# Get your token from: https://mailtrap.io/api-tokens
MAIL_SERVICE=mailtrap
MAILTRAP_TOKEN=your-mailtrap-api-token
MAILTRAP_FROM_EMAIL=hello@demomailtrap.com
MAILTRAP_FROM_NAME=Reminders App
DEFAULT_EMAIL=your-default-email@example.com

# QStash (Upstash) – Free scheduler
# Get these from console.upstash.com -> QStash
QSTASH_URL=https://qstash-us-east-1.upstash.io
QSTASH_TOKEN=your-qstash-token
QSTASH_CURRENT_SIGNING_KEY=sig_your-current-key
QSTASH_NEXT_SIGNING_KEY=sig_your-next-key

# Webhook URL – the public URL where QStash will POST alerts.
# On Render, this is your service URL (e.g. https://your-app.onrender.com).
WEBHOOK_BASE_URL=https://your-app.onrender.com

# Database – Turso (cloud SQLite via libsql)
# Leave both blank for local development (uses file:reminders.db automatically).
# Set both in production so the server persists data across Render restarts.
# Get these from https://app.turso.tech after creating a database.
TURSO_DATABASE_URL=libsql://your-db-name-your-org.turso.io
TURSO_AUTH_TOKEN=eyJhbGciOiJIUzI1NiI...

# Scheduler mode
# true  = local polling (dev)   – setInterval checks reminders every SCHEDULER_INTERVAL ms.
# false = QStash webhooks (prod) – no polling; QStash wakes the server when alerts are due.
USE_POLLING=true
SCHEDULER_INTERVAL=3000
